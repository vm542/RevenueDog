package com.revenuedog.purchases

/** Verbosity of the SDK's logging, set via [Purchases.logLevel]. Default is [INFO]. */
enum class LogLevel {
    VERBOSE,
    DEBUG,
    INFO,
    WARN,
    ERROR
}
